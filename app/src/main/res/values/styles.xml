<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="myRatingBar" parent="@android:style/Widget.Material.RatingBar.Small">
        <item name="android:progressDrawable">@drawable/rate1</item>
        <item name="android:indeterminateDrawable">@drawable/rate3</item>
        <item name="android:numStars">10</item>
    </style>

    <style name="NavigationDrawerStyle">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:gravity">right</item>
    </style>

    <style name="CustomTabTextSryle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">18sp</item>
        <item name="android:textAllCaps">false</item>
    </style>


    <style name="DateTabTextSryle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:textSize">23sp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:state_multiline">true</item>
        <item name="android:maxLines">3</item>
    </style>

</resources>
